[project]
name = "specl-discover"
version = "0.1.0"
description = "LLM-guided algorithm discovery with Specl model checker"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
]

[project.scripts]
discover = "main:main"
